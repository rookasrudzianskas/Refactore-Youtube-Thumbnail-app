import React from 'react';
import "./styles/VideoCard.css";

const VideoCard = ({ thumbnailUrl, title, channelName, numOfViews, postedDate }) => {
    return (
        <div className="videoCard">

        </div>
    );
};

export default VideoCard;
